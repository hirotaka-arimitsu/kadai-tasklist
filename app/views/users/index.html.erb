<% if @users.any? %>
  <ul class="list-unstyled">
    <% @users.each do |user| %>
      <li class = "media">
        <div class = "media-body">
          <%= user.name %>
        </div>
      </li>
    <% end %>
  </ul>
  <%= paginate @users %>
<% end %>